{
    "name": "root",
    "private": true,
    "scripts": {
        "test": "lerna run test --",
        "release": "lerna publish"
    },
    "workspaces": [
        "packages/*"
    ],
    "devDependencies": {
        "lerna": "^3.19.0"
    }
}
